source 'http://rubygems.org'

#gem 'rails', :path => "lib/"

# Bundle edge Rails instead:
gem 'rails', :git => 'git://github.com/rails/rails.git'
gem "sprockets", :git => "git://github.com/sstephenson/sprockets.git"


# Asset template engines
gem 'json'
gem 'sass'
gem 'coffee-script'
gem 'uglifier'
gem 'jquery-rails'


# DB
gem 'sqlite3'

# AWS
gem 'aws-s3', :require => 'aws/s3'  # do I need to roll my own wrapper to use local/s3 depending 

# Authentication / Authorization
gem 'devise'
gem 'cancan'

# View frameworks
gem 'formtastic'
gem 'slim'
#gem 'slim-rails'

# rails_admin for simple admin-level CRUDs for all models.
#gem 'rails_admin', :git => 'git://github.com/sferik/rails_admin.git'

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

 group :development, :test do
#  gem 'webrat'
  gem 'ruby-debug'
#  gem 'ruby-prof'
   gem 'shoulda'
   gem 'annotate-models'
   gem 'rails-footnotes', '>= 3.7', :group => :development
 end

